<%= form_for(@manage_expense) do |f| %>
  <% if @manage_expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manage_expense.errors.count, "error") %> prohibited this manage_expense from being saved:</h2>

      <ul>
      <% @manage_expense.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :total_amount %><br />
    <%= f.number_field :total_amount %>
  </div>
  <div class="field">
    <%= f.label :divide_among %><br />
    <%= f.number_field :divide_among %>
  </div>
  <div class="field">
    <%= f.label :people_paid %><br />
    <%= f.number_field :people_paid %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, :rows => 3 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
